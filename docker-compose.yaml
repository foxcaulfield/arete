services:
    db:
        image: postgres:17
        env_file: .env
        ports:
            - "${DATABASE_PORT}:${DATABASE_PORT}"
        volumes:
            - db_data:/var/lib/postgresql/data
    
    backend:
        build: .
        env_file: .env
        ports:
            - "${PORT}:${PORT}"
        depends_on:
            - db
volumes:
    db_data:
        driver: local
