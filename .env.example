# Database configuration
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# POSTGRES_USER=your_postgres_user
# POSTGRES_PASSWORD=your_postgres_password
# POSTGRES_DB=your_database_name

# Constructed DATABASE_URL (or set directly)
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${POSTGRES_DB}?schema=public

# Alternative direct DATABASE_URL (uncomment if preferred)
DATABASE_URL=postgresql://user:password@localhost:5432/your_db?schema=public

# Application port
PORT=3000

# JWT secret for authentication
JWT_SECRET=your_jwt_secret_here
COOKIE_PREFIX=your_cookie_prefix_here
FRONTEND_ORIGIN=http://localhost:0000